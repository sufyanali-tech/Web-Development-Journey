// Expression 

// Expression is a lines of code which produce some value For Example

const value = 2 + 2;   // 2 + 2 Produce some value this is called Expression

// Statement 

// Statement is also a big lines of codes which are like full sentence that translate our actions For Example

if (value > 1) {
    
    console.log("Value is greater than 1");
    
} else {

    console.log("Value is not greater than 1"); // This statement tell us something
    
}