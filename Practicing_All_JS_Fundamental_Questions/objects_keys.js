// All keys of the object enter in the array

let obj = {
    myName : "Sufyan Ali",
    age : 19,
    email : "test@test.com"
}

let object_keys = Object.keys(obj);

console.log(object_keys);

// Object Entries Arrays of Arrays

let object_entries = Object.entries(obj);

console.log(object_entries);
