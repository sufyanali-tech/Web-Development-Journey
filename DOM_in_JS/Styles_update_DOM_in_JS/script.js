// JS say CSS badalna

let h1 = document.querySelector("h1");

// h1.classList.add("hello") // Adding the css class to the element 
// h1.classList.remove("hello") // Adding the css class to the element 
h1.classList.toggle("hello") // Adding the css class to the element 

