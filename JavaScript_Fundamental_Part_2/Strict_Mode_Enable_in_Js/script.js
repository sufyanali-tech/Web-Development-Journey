"use strict"; // if we don't use the strict mode then javaScript will not show the error in console which cause the accidental error in out program

let variable = "Sufyan Ali";

if(variable) {

    variables = "Ali"  
}

console.log(variable);

